name: Sync to Public Pages Repo

on:
  push:
    branches:
      - master

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout private repo
        uses: actions/checkout@v4

      - name: Checkout public repo
        uses: actions/checkout@v4
        with:
          repository: Xiaogang206/gang
          token: ${{ secrets.PUBLISH_TOKEN }}
          path: public

      - name: Sync markdown files
        run: |
          rm -rf public/*
          cp -r *.md public/ || true
          cp -r */ public/ || true
          rm -rf public/public  # ✅ 防止嵌套 public 文件夹

      - name: Commit and push changes (force rebuild)
        run: |
          cd public
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Auto sync from private repo (force rebuild)" --allow-empty
          git push
